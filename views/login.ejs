<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Site de Pulverização</title>
    <style>
        /* Estilos básicos e fundo temático */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background: url('https://example.com/imagem-pulverizacao.jpg') no-repeat center center fixed;
            background-size: cover;
            background-color: #000000;
        }
        
        /* Container de login com leve transparência */
        .login-container {
            width: 350px;
            padding: 25px;
            background-color: rgba(0, 0, 0, 0.85); /* Fundo semi-transparente para contraste */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
            border-radius: 8px;
            text-align: center;
            color: #ffffff;
        }
        
        /* Título estilizado */
        h2 {
            margin-bottom: 20px;
            color: #00c853; /* Verde para o título */
        }
        
        /* Campos de entrada */
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #00c853; /* Bordas verdes */
            border-radius: 4px;
            background-color: #333333; /* Fundo do campo de entrada */
            color: #ffffff;
            box-sizing: border-box;
        }
        
        /* Placeholder em tom mais claro */
        input[type="text"]::placeholder, input[type="password"]::placeholder {
            color: #aaaaaa;
        }
        
        /* Botão de login */
        button {
            width: 100%;
            padding: 12px;
            background-color: #00c853; /* Verde para o botão */
            color: #ffffff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #009624; /* Escurecimento no hover */
        }
        
        /* Link para cadastro */
        .signup-link {
            margin-top: 15px;
            display: block;
            color: #00c853;
            text-decoration: none;
            font-size: 14px;
        }
        .signup-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<div class="login-container">
    <h2>Login</h2>
    <form action="/login" method="POST">
        <input type="text" name="username" placeholder="Nome de usuário" required>
        <input type="password" name="password" placeholder="Senha" required>
        <button type="submit">Entrar</button>
    </form>
    <a href="/cadastro" class="signup-link">Ainda não tem uma conta? Cadastre-se</a>
</div>

</body>
</html>
